# 鎖定版本：雲端（Python 3.11）用 numpy 2.x + pyarrow 新版會間歇性原生崩潰
# （double free / segfault → data.py 匯入 pandas 時掛掉，表面顯示 KeyError: 'data'）。
# 改用 numpy 1.x 這組久經考驗的穩定組合，消除 ABI 不相容。
streamlit==1.55.0
pandas==2.2.3
numpy==1.26.4
pyarrow==17.0.0
plotly>=5.18
google-api-python-client>=2.100
google-auth>=2.23
